# deploy

Production deployment helpers.

These scripts are now the primary deployment entry points:

- `deploy.sh`: full local build + docker deploy
- `deploy-local-build.sh`: local build helper
- `deploy-to-server.sh`: server-side compose deployment
- `deploy-jenkins.sh`: Jenkins deployment helper
