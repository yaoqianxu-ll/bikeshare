# ssl

HTTPS related files for production deployment.

Structure:

- `env.example`: SSL related environment variable example
- `nginx-ssl.conf`: sample HTTPS nginx config
- `start.sh`: helper to start SSL compose
- `ssl/`: certificate placeholders

Replace the placeholder certificate files before real deployment.
