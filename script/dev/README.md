# dev

Development and test environment files.

Structure:

- `application.yml`: local backend configuration snapshot
- `docker-compose.yml`: local infrastructure compose
- `env.example`: local environment variable example
- `start.sh` / `start.bat`: one-click local infrastructure startup
